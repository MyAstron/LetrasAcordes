package com.letrasacordes.application.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta extraída del Logo
val AzulProfundo = Color(0xFF1A237E) // El azul más oscuro del borde
val AzulMedio = Color(0xFF283593)
val CianBrillante = Color(0xFF4FC3F7) // El celeste del centro
val BlancoPapel = Color(0xFFF5F5F5)
val PlataBrillante = Color(0xFFE0E0E0)

// Colores de Acento y Estado
val AcordeAmarillo = Color(0xFFFFEB3B) // Para que los acordes resalten sobre el azul
val ErrorRojo = Color(0xFFD32F2F)

// Tema Oscuro (Ajustado al logo)
val DeepNavy = Color(0xFF0D1B2A)
val SurfaceDark = Color(0xFF1B263B)
